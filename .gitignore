# Model weights / engines
checkpoints/
*.engine

# Outputs (keep the directory placeholder)
outputs/*
!outputs/.gitkeep
!outputs/benchmark.json
!outputs/benchmark.md
results/

# Private source images (sprites are derived from the user's drawings; setup.sh copies them locally)
assets/source/*
assets/sprites/*/

# Python
__pycache__/
*.pyc
.venv/

# Logs / WSL cruft
*.log
*Zone.Identifier

# Claude Code harness (agent worktrees live here)
.claude/
