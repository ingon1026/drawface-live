# Model weights / engines
checkpoints/
*.engine

# Outputs (keep the directory placeholder)
outputs/*
!outputs/.gitkeep
!outputs/benchmark.json
!outputs/benchmark.md
results/

# Private source images (sprites are derived from the user's drawing; setup.sh copies them locally)
assets/source/*
assets/sprites/pig/

# Python
__pycache__/
*.pyc
.venv/

# Logs / WSL cruft
*.log
*Zone.Identifier
